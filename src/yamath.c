// #include "yagl.h"

// I'm not sure in these functions, so just left them here (I've just tryed 3D if you're interested)
// float Vec3Dot(Vec3 v1, Vec3 v2) { return v1.x * v2.x + v1.y * v2.y + v1.z * v2.z; }
// Vec3 Vec3Add(Vec3 v1, Vec3 v2) { return (Vec3){v1.x + v2.x, v1.y + v2.y, v1.z + v2.z}; }
// Vec3 Vec3Subtract(Vec3 v1, Vec3 v2) { return (Vec3){v1.x - v2.x, v1.y - v2.y, v1.z - v2.z}; }
// Vec3 Vec3Scale(Vec3 v, float scale) { return (Vec3){v.x * scale, v.y * scale, v.z * scale}; }
// Vec3 Vec3Cross(Vec3 v1, Vec3 v2) {
//     Vec3 result;
//     result.x = v1.y * v2.z - v1.z * v2.y;
//     result.y = v1.z * v2.x - v1.x * v2.z;
//     result.z = v1.x * v2.y - v1.y * v2.x;
//     return result;
// }
